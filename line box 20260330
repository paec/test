| 概念 / 属性            | 定义 / 是什么                                                                 | 适用对象                                        | 对自身高度的影响                                                   | 对 line box 高度的影响                                               | 常见用途 / 备注                                                                                 |
|------------------------|------------------------------------------------------------------------------|------------------------------------------------|--------------------------------------------------------------------|----------------------------------------------------------------------|--------------------------------------------------------------------------------------------------|
| **inline box**         | 行内盒子：由 `display: inline` 元素（及匿名文本）在行内产生的盒子            | `span`, `a`, `b`, 纯文字生成的匿名 inline 等   | Inline box 的行高參與值（用於 line box 計算的部分）只由 line-height 決定；padding/border 只影響視覺繪製範圍，不進入行高計算。 | 作为构成 line box 的基本单元；所有 inline box 的几何边界共同决定 line box 高度 | 每个行内元素在“一行中的占位”。基线（baseline）对齐，是内联排版的基础单位。                   |
| **line box**           | 行盒：一行内容的“整体容器”                                                    | 由父 block 容器中一行一行排版自动生成          | 本身不直接设样式，高度 = 行内所有盒子的包络边界                    | 高度必须足够容纳这一行所有 inline-level box 的可视区域              | 一段文本有多行 → 有多个 line box，每个代表一行。段落的“行距”就是多个 line box 垂直属叠的效果。 |
| **block 容器**         | 包含行盒的块级元素                                                            | `div`, `p`, `li`, `section` 等                 | 高度 = 所有 line box 的高度之和 + 上下 padding + border（margin 在盒子外部，不算入自身高度）   | 自身不直接生成 line box，但作为行盒的容器                            | 常在这里设 `line-height` 来统一控制段落的行距。                                                |
| **text node（文字）**  | 文本节点本身，没有 CSS 盒模型                                                 | HTML 文本内容                                  | 不直接有 CSS 属性；参与排版时变成匿名 inline box                   | 通过父元素继承的 `font-size` / `line-height` 产生匿名 inline box 影响行高 | 你看到的“纯文字”都是以匿名 inline box 形式参与行盒计算。                                      |
| **line-height** | 行高：控制文字所在的行內高度（字的上下空間）    | 任何元素（inline / inline-block / block） | 決定該元素對應的 inline box 的行高區域，以及文字在此區域中的上下留白分布         | 同一行中，瀏覽器會比較該行所有 inline‑level 元素的行高設定，**以其中「最高」的行高為準**，作為這一行 line box 的實際高度 | 排版行距的核心屬性；通常寫在外層 block 上做為預設行高，也可在單一 inline / inline‑block 上設更大的值來局部拉高該行。 |
| **line-height: 数字**  | 相对值：例如 `line-height: 1.5;` → 倍数 × `font-size`                        | 任何元素                                       | 计算为 `font-size * 倍数`，易于继承                                 | 统一比例放大或缩小行高，影响该元素及子元素形成的所有行               | 最推荐写法：相对字体大小，适应不同字号。                                                        |
| **line-height: 长度**  | 绝对值：如 `line-height: 24px;`                                               | 任何元素                                       | 直接固定该元素的 inline box 高度                                   | 一行中谁的 inline box 更高，谁就更影响 line box 高度                 | 适用于图标、按钮、单行垂直置中等，需要“固定高度”的场景。                                      |
| **inline 元素**        | 行内元素：不会换行，占据内容自身大小                                          | `span`, `a`, `strong` 等                       | 行高參與值只由 font-size + line-height 決定；垂直 padding/border 只影響視覺繪製範圍，不進入行高計算。           | 其几何边界（含 padding/border）需被 line box 包含，**可以影响 line box 高度**（但常被现有行高“吸收”，视觉不明显） | 文本内小片段、高亮、链接等。上下 padding/border 多落在已有行高空间里，看起来像“没撑高行距”。   |
| **inline-block 元素**  | 行内块：在行内排列，但自身是一个块状盒子                                      | `display: inline-block`                        | 高度 = 内容 + padding + border（受自身 line-height 影响内部文字） | 作为一个整体块参与行盒，高度非常直观地撑开该行的 line box           | 做小按钮、icon、badge 等；padding/border 撑高视效非常明显。                                    |
| **padding（上下）**      | 內容與邊框之間的內距（垂直方向）       | inline / inline-block / block 等 | **inline**：不改變參與行高計算的高度，只讓自身視覺盒子向上下延伸；<br>**inline-block / block**：會增加元素幾何高度（content + padding-top + padding-bottom） | **inline**：瀏覽器計算 line box 高度時仍以 `line-height` / 字型 metrics 為主，padding 不會把該行撐高；<br>**inline-block / block**：padding 會讓元素盒子變高，line box 必須變高以容納它 | 要「明確撐高行高」請用 `line-height` 或 `inline-block / block + padding`；純 `inline` 的上下 padding 只會讓元素看起來變厚或跨行，不會改變單行高度。 |
| **border（上下）**       | 元素邊框（垂直方向）                  | inline / inline-block / block 等 | **inline**：不改變參與行高計算的高度，只在當前行的上下畫出額外邊線；<br>**inline-block / block**：會增加元素幾何高度（再加上 border-top / border-bottom） | **inline**：與 padding 類似，border 不會改變該行的 line box 高度，只是畫在行內或跨越多行；<br>**inline-block / block**：border 會隨著元素盒子一起撐高 line box | 在實作上，inline 的上下 border 常「包在」原本的行高範圍內，看起來只是多一條線；inline-block / block 的 border 則會明顯撐高所在那一行。 
| **margin（上下）** | 元素與相鄰元素之間的外距（垂直方向） | inline / inline-block / block 等 | **inline**：垂直 margin **完全無效**，不會推開上下相鄰內容，也不影響自身視覺大小；<br>**inline-block / block**：會在元素盒子外部產生實質間距，推開上下相鄰元素 | **inline**：不影響 line box 高度，瀏覽器直接忽略垂直 margin；<br>**inline-block / block**：margin 在 line box **外部**，不計入 line box 高度本身，但會推開相鄰的 line box / 元素，造成視覺上的間距 | inline 的垂直 margin 是 CSS 中少數「寫了完全沒作用」的屬性組合；要控制 inline 元素的上下間距，只能靠 `line-height` 或改用 `inline-block`。 
| **baseline（基线）**   | 字符排版中的基准线，文字对齐常用的参照                                       | inline-level 内容                              | Inline-block 的 baseline 位置會受內容影響（有文字時在最後一行文字的 baseline；無文字時在 margin-bottom 底部），這點與純 inline 不同。                               | 行内文字通常以 baseline 对齐，不直接决定 line box 高度               | 在同一行里，不同字体/字号通常以基线对齐；行高改变的是基线上下的空间，而非基线本身位置。        |
| **段落行距**           | 多个 line box 在垂直方向上的间距感觉（肉眼看到的“行与行之间的距离”）          | 包含多行的 block 容器                          | 由每一行的 line box 高度決定；line box 高度取該行所有 inline-level box 中最高者：純 inline 內容由 line-height 決定，inline-block 由 content + padding + border 決定。視覺上的行距感是相鄰 line box 緊密疊放的結果，並非來自 margin。          | 是所有 line box 高度的叠加效果                                     | 通常通过在容器/文本上设合适的 `line-height` 来控制整体阅读体验。                              |



| 屬性                | 在 Inline 元素（span, a）                                                                               | 在 Inline-Block 元素                                                                                     |
|---------------------|---------------------------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------|
| **line-height**     | **唯一真神**。直接決定該行的行高（line box 高度）。文字、背景、padding 都被限制在這個行高框架裡。       | **基本盤**。決定元素參與行內排版時的「最小高度」，但實際行高還會再加上 padding / border 等。           |
| **font-size**       | **視覺幻影**。字看起來變大、會蓋到上下行，但不會改變這一行的 line box 高度（行高仍由 line-height 決定）。 | **實體支柱**。在 `height: auto` 時，字變大會撐高盒子本身，高盒子會實際推開上下行，讓 line box 變厚。   |
| **padding（垂直）** | **視覺裝飾**。背景色和邊界會向上下延伸，看起來變厚、甚至跨行；但不改變該行的 line box 高度。            | **實體空間**。會確實增加盒子幾何高度（content + padding），line box 必須變高來容納它，行距會被撐開。   |
| **height: auto**    | 對排版高度 **基本無效**。inline 元素無法透過 `height` 直接控制自身在行內的高度，仍以 line-height 為準。 | **連鎖反應**。`height: auto` 時，高度由內容（字體大小、行高、padding 等）決定，盒子越高，該行 line box 就越厚。 |


| 概念              | 說明 |
|-------------------|------|
| **inline-block**  | 同時具有 inline 與 block 的特性：在排版上像 inline 一樣與文字排在同一行、參與同一個 line box；又像 block 一樣可以設定寬高，padding / margin 在垂直方向具有實質空間效果，能把所在那一行的 line box 撐高並推開上下內容。 |

1. **Inline box 的大小**

   以 display: inline 的元素為例，它的「行高參與值」（用於 line box 計算的高度）由：

   - line-height 決定行高區域的總高度
   - font-size 決定字型 metrics（ascender / descender），
   影響文字在行高區域內的上下分布位置

   而：
   - 垂直 padding / border：只改變視覺繪製範圍，
   不進入「這一行有多高」的計算。

   所以 inline box 可以視覺上變得比這一行高（蓋到上下行），
   但這不代表那一行的 line box 高度變大。

---

2. **Line box 的大小**

   一個 line box（某一行）高度是瀏覽器算出來的，
   取該行所有 inline-level box 中「最高的那個」：

   - 純 inline box      → 高度由 line-height 決定
   - inline-block box   → 高度由 content + padding + border 決定
   （margin 在盒子外部，不計入 line box 高度）

   同一行裡如果有人特別高，那一行就會被它撐開。
   外層 block 設的 line-height 只是預設值，
   遇到更高的子元素會被「超車」。

   補充：
   - 垂直 padding / border 對純 inline 不影響 line box 高度
   - 垂直 margin 對任何元素都不影響 line box 高度
   （margin 永遠在 line box 外部）

---

3. **一句話總結**

   Inline box 的「視覺大小」（尤其是垂直 padding / border）
   可以跟它所在行的 line box 高度不一樣。 <br><br>


   line box 的高度取決於：
   - 該行 inline box      → 由 line-height 決定
   - 該行 inline-block    → 由 content + padding + border 決定
   兩者取最高值，而不是簡單等於「那個元素看起來有多高」。
